# Copy to `.env` and fill in. The app reads NEWSAPI_KEY from the environment.
# Without a key, forecasts still run — the sentiment tilt simply degrades to
# "no news" (no adjustment).

# Get a free key at https://newsapi.org  (free tier: ~last 30 days of articles)
NEWSAPI_KEY=
